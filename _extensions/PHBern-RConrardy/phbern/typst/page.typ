// Page layout is defined by the PHBern template in typst-template.typ.
// This partial intentionally replaces Quarto's default page partial so the
// normalized `logo` metadata can be used in the institutional header without
// also adding Quarto's default full-page logo background.
